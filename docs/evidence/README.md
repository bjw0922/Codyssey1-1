# Evidence Placeholders

- Add a browser screenshot that shows the address bar with the mapped port after you verify `http://localhost:8088` or `http://localhost:8090`.
- Add a VSCode screenshot that shows GitHub sign-in and repository integration.
- Mask any token, code, email address, or private identifier before submission.
